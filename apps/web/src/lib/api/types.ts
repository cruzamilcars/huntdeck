// Backwards-compatible re-export of the canonical contract.
// Single source of truth: packages/shared/src/contract.ts (@huntdeck/shared).
export type {
  InvestigationResponse,
  IocType,
  ParsedIoc,
  ProviderStatus,
  Severity,
} from "@huntdeck/shared";

